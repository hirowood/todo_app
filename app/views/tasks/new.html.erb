<h1>新しいToDoを作成</h1>
<%= form_with(model: @task, local: true) do |f| %>
  <div>
    <%= f.label :title, "タイトル" %>
    <%= f.text_field :title %>
  </div>
  <div>
    <%= f.label :completed, "完了"%>
    <%= f.check_box :completed %>
  </div>

  <div>
    <%= f.submit "作成する" %>
  </div>
<% end %>

<%= link_to "戻る", tasks_path %>